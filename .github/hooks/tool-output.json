{
  "version": 1,
  "hooks": {
    "postToolUse": [
      {
        "type": "command",
        "bash": "cat > tmp$(date +%s).json",
        "powershell": "$input | Set-Content \"tmp$([DateTimeOffset]::UtcNow.ToUnixTimeSeconds()).json\""
      }
    ]
  }
}
