{
  "version": 1,
  "hooks": {
    "postToolUse": [
      {
        "type": "command",
        "powershell": "if ($env:TOOL_NAME -eq 'create' -or $env:TOOL_NAME -eq 'edit') { npx prettier --write . }"
      }
    ]
  }
}
